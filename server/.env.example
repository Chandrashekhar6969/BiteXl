# Example environment variables for InfoHub server
# Copy to .env and edit if you need to change the port
PORT=3001
# No API keys are required for the public free APIs used (open-meteo, exchangerate.host)
